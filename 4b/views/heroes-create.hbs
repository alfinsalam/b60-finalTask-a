{{#> layout currentPage="Add Hero"}}
    <div class="container-form">
      <div class="bg-form">
        <h1 class="form-title">Add Hero</h1>
        <form
          class="form"
          action="/heroes/add"
          method="POST"
          enctype="multipart/form-data"
        >
          <div class="form-group">
            <label for="name">Hero Name</label>
            <input
              type="text"
              id="name"
              name="name"
              placeholder="Enter hero name..."
              required
            />
          </div>

          <div class="form-group">
            <label for="type">Hero Type</label>
            <select id="type" name="type_id" required>
              {{#each types}}
                <option value="{{this.id}}">{{this.name}}</option>
              {{/each}}
            </select>
          </div>

          <div class="form-group">
            <label for="image">Upload Image</label>
            {{#if hero.photo}}
              <img src="{{hero.photo}}" alt="image hero" width="200" />
            {{else}}
              <p>No image available</p>
            {{/if}}
            <input
              type="file"
              id="image"
              name="photo"
              accept="image/*"
            
            />
          </div>

          <button type="submit" class="submit-btn">Submit</button>
        </form>
      </div>
    </div>
{{/layout}}
